# This exercise teaches storage of a single variable
# It also covers the reserved words as a warning to new users
# It explains how new variables are visible in the Environment Pane in RStudio
name <- "Username"

# It then has us assign a new value to the variable
name <- "Darrell"

# It shows how variable calls can be used in other functions through a print
# demonstration
print(name)
