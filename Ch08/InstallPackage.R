## This exercise demonstrates the installation of packages and dependencies

# install ggplot2 and its dependencies
install.packages("ggplot2", dep = T)

# load the package
library(ggplot2)

# open the help plot for a package command
help(qplot)
